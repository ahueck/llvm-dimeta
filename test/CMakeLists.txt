add_subdirectory(verifier)

dimeta_find_llvm_progs(DIMETA_FILECHECK_EXEC "FileCheck-${LLVM_VERSION_MAJOR};FileCheck" ABORT_IF_MISSING)

if(LLVM_EXTERNAL_LIT)
  cmake_path(GET LLVM_EXTERNAL_LIT PARENT_PATH LLVM_EXTERNAL_LIT_DIR)
endif()

dimeta_find_llvm_progs(DIMETA_LIT_EXEC
  "llvm-lit;lit;lit.py"
  HINTS ${LLVM_EXTERNAL_LIT_DIR} /usr/lib/llvm-${LLVM_VERSION_MAJOR}/build/utils/lit/ /usr/bin /usr/local/bin /opt/local/bin
  ABORT_IF_MISSING
)

macro(pythonize_bool truth_var var)
  if(${truth_var})
    set(${var} True)
  else()
    set(${var} False)
  endif()
endmacro()

function(configure_dimeta_lit_site_cfg input output)
  set(LIT_SITE_CFG_IN_HEADER
      "## Autogenerated for dimeta from ${input}\n## Do not edit!"
  )
  
  set(DIMETA_PROJECT_DIR ${PROJECT_SOURCE_DIR})
  set(DIMETA_LIBRARY_DIR ${CMAKE_BINARY_DIR}/lib)
  set(DIMETA_PASS_DIR ${DIMETA_LIBRARY_DIR}/pass)
  set(DIMETA_TEST_PASS_DIR ${CMAKE_CURRENT_BINARY_DIR}/verifier)
  
  set(DIMETA_SCRIPT_DIR ${CMAKE_BINARY_DIR}/script)

  pythonize_bool(DIMETA_USE_HEAPALLOCSITE DIMETA_HEAPALLOCSITE)
  pythonize_bool(DIMETA_USE_TBAA DIMETA_TBAA)
  pythonize_bool(DIMETA_ENABLE_FLANG DIMETA_WITH_FLANG)
  
  if(${LLVM_VERSION_MAJOR} VERSION_GREATER_EQUAL "13")
    #set(DIMETA_OPT_ARGS "-enable-new-pm=0")
  endif()

  set(DIMETA_OPT_SHIM ${CMAKE_BINARY_DIR}/test/verifier/opt-shim.sh)

  dimeta_target_generate_file(${input} ${output})
endfunction()

configure_dimeta_lit_site_cfg(
  ${CMAKE_CURRENT_SOURCE_DIR}/lit.site.cfg.in
  ${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg
)

function(dimeta_add_lit_testsuite target comment)
  cmake_parse_arguments(ARG "" "" "PARAMS;DEPENDS;ARGS" ${ARGN})

  foreach(param ${ARG_PARAMS})
    list(APPEND TEST_PARAMS --param ${param})
  endforeach()

  add_custom_target(${target}
    COMMAND ${DIMETA_LIT_EXEC} ${ARG_ARGS} ${TEST_PARAMS} ${ARG_UNPARSED_ARGUMENTS}
    COMMENT "${comment}"
    USES_TERMINAL
  )

  if (ARG_DEPENDS)
    add_dependencies(${target} ${ARG_DEPENDS})
  endif()
endfunction()

function(dimeta_add_lit_target)
  cmake_parse_arguments(ARG "" "" "SUITES;THREADS" ${ARGN})

  foreach(suite IN LISTS ARG_SUITES)
    if("${suite}" STREQUAL "all")
      set(SUITE_PATH ${CMAKE_CURRENT_SOURCE_DIR})
      set(TARGET_NAME check-dimeta)
    else()
      set(SUITE_PATH ${CMAKE_CURRENT_SOURCE_DIR}/${suite})
      string(REPLACE "/" "_" suite_target ${suite})
      set(TARGET_NAME check-dimeta-${suite_target})
    endif()

    if(NOT EXISTS ${SUITE_PATH})
      if(NOT ${SUITE_PATH} MATCHES "staging")
        message(WARNING "Could not find suitable lit test target at ${SUITE_PATH}")
      endif()
      continue()
    endif()

    dimeta_add_lit_testsuite(${TARGET_NAME}
      "Running the lit suite dimeta::${suite}"
      ${SUITE_PATH}
      ARGS --show-xfail --show-unsupported -v -j ${ARG_THREADS}
      PARAMS dimeta_site_config=${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg
      DEPENDS ${DIMETA_TEST_DEPENDS}
    )
  endforeach()
endfunction()

set(DIMETA_TEST_DEPENDS dimeta::Types dimeta::TestPass)

set(DIMETA_SUITES
  all
  pass
  pass/gep
  pass/pointer
  pass/ir
  pass/staging
  pass/inheritance
  pass/call
  pass/cuda
  pass/fortran
)

include(ProcessorCount)
ProcessorCount(NUM_CPU)
if(NUM_CPU EQUAL 0)
  set(NUM_CPU 1)
endif()

dimeta_add_lit_target(SUITES ${DIMETA_SUITES} THREADS ${NUM_CPU})

add_test(
  NAME dimeta-lit-suite
  COMMAND
    ${DIMETA_LIT_EXEC} -j ${NUM_CPU} --show-xfail --show-unsupported --param
    dimeta_site_config=${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg
    ${CMAKE_CURRENT_SOURCE_DIR}
)

dimeta_target_format(
  format-dimeta-tests "Formats project test files"
  TARGETS *.c *.cpp *.h
)

if(DIMETA_TEST_CONFIGURE_IDE)
  function(add_test_target_ide ARG_TARGET)
    cmake_parse_arguments(ARG "" "COMPILE_STD;LINKER_LANGUAGE" "HEADER;SOURCES" ${ARGN})

    if(NOT ARG_SOURCES)
      return()
    endif()

    add_executable(${ARG_TARGET} EXCLUDE_FROM_ALL ${ARG_HEADER} ${ARG_SOURCES})

    target_include_directories(
      ${ARG_TARGET} PRIVATE ${PROJECT_SOURCE_DIR}/lib/pass
                        ${PROJECT_SOURCE_DIR}/lib
                        ${PROJECT_SOURCE_DIR}
    )
    if(ARG_COMPILE_STD)
      target_compile_features(${ARG_TARGET} PUBLIC ${ARG_COMPILE_STD})
    endif()
    set_target_properties(${ARG_TARGET} PROPERTIES LINKER_LANGUAGE ${ARG_LINKER_LANGUAGE})
  endfunction()

  file(GLOB_RECURSE DIMETA_CXX_TESTS CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/pass/*.cpp)
  file(GLOB_RECURSE DIMETA_CC_TESTS CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/pass/*.c)
  file(GLOB_RECURSE DIMETA_HEADER_TESTS CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/pass/*.h)
  file(GLOB_RECURSE DIMETA_FORTRAN_TESTS CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/pass/*.f90)

  add_test_target_ide(dimeta_cxx_test_objects
    HEADER "${DIMETA_HEADER_TESTS}"
    SOURCES "${DIMETA_CXX_TESTS}"
    COMPILE_STD cxx_std_17
    LINKER_LANGUAGE CXX
  )
  add_test_target_ide(dimeta_cc_test_objects
    HEADER "${DIMETA_HEADER_TESTS}"
    SOURCES "${DIMETA_CC_TESTS}"
    COMPILE_STD c_std_99
    LINKER_LANGUAGE C
  )
  # add_test_target_ide(dimeta_fortran_test_objects
  #   SOURCES "${DIMETA_FORTRAN_TESTS}"
  #   LINKER_LANGUAGE Fortran
  # )
endif()
